# AI-Learn 仓库工作指令

## 1. 文件作用

本文件是写给 Codex 和其他编码 Agent 的仓库级工作手册。Agent 进入仓库后应在执行任务前读取它；它不会被 Python、Next.js、Docker 或数据库执行，也不会直接改变应用运行时。

- 默认使用简体中文沟通、写文档、日志和用户界面文本。
- 变量名、函数名、类名、API 路径等代码标识使用英文。
- Git commit message 使用英文 Conventional Commits。
- 项目根目录始终指当前 Git 仓库根目录，不使用本机绝对路径作为规则。

## 2. 当前项目事实

- 后端：FastAPI、SQLAlchemy、Alembic、PostgreSQL、Redis。
- 前端：Next.js 14、React、TypeScript、Tailwind CSS。
- 本地编排：Docker Compose；常用命令见 `Makefile` 和 `README.md`。
- AI 出题固定为“出题 Agent → 审题 Agent”两层，允许有限的生成、审题、修订重试，但不得增加第三层出题 Agent。
- TutorHarness 的四个辅导角色与出题流水线彼此独立，辅导角色不得参与题目生成或审题。
- 默认不进行网页搜索、爬取或外部教材检索；只有任务明确授权时才可使用。外部 LLM Provider 调用不等同于网页搜索。

发生冲突时，按以下顺序判断：

1. 当前代码、模型、迁移、配置和可执行测试是实现事实。
2. 本 `AGENTS.md` 是工作流程与安全规则。
3. `docs/prompts_3/` 是当前已实施功能的主要设计依据，但必须用代码重新核验。
4. `docs/prompts_4/` 是尚未完成的优化实施包；提示词存在不代表功能已实现。
5. `docs/prompts_1/` 只提供早期产品背景。
6. `docs/prompts_2（已废弃）/` 只作历史参考；不得恢复其中的八层出题、PID、FeedbackAggregator 或 SummaryAgent 自进化架构。

设计文档、HTML 报告、旧文件路径和行号都只是调查线索。若与当前代码不一致，先报告差异，再按当前事实制定最小修改方案。

## 3. 标准工作流程

所有修改遵循：

```text
调查影响范围 → 输出计划与风险 → 实施 → 自主验收 → 迭代修复 → 报告证据
```

开始前必须：

1. 完整读取相关文件和目录级 `AGENTS.md`（如果存在），并检查 `git status`。
2. 找出调用链、测试、配置、数据结构和关联模块；修改前再次读取目标文件的最新内容。
3. 涉及两个及以上文件或模块时，先列出可跟踪的计划和允许修改的文件范围。
4. 保护工作区已有变更，不覆盖、回退、暂存或提交不属于当前任务的修改。
5. 功能新增和缺陷修复优先使用红—绿—重构流程，补充覆盖真实行为的测试。

风险等级：

- 低：只读调查、文本修正、可逆的局部代码修改。
- 中：跨模块契约、依赖、构建或容器编排调整。
- 高：删除、认证授权、数据库结构/数据、密钥、生产配置、部署和不可逆外部操作。

高风险操作必须先核验精确目标、影响范围、备份和回滚路径，并取得用户明确授权。用户已经明确授权的具体操作无需重复询问，但不得扩大范围。

## 4. 文件与安全边界

- 所有最终交付物放在项目目录内；临时脚本和调试产物放入工作区临时目录。
- 不主动创建 README、`.env.example` 或无关说明文档，除非任务明确要求。
- 删除文件或模块前全局搜索 import、路由、配置、变量、构建脚本、Nginx、环境变量和文档引用；确认目标存在后再删除。
- 永远不提交 `.env`、令牌、密码、私钥、凭证文件或真实个人数据。
- 不使用 `git reset --hard`、`git clean`、强制推送等破坏性 Git 操作，除非用户对精确目标明确授权。
- 未经明确要求，不推送远端、不创建 PR、不修改外部系统状态。

跨模块修改必须逐项检查：

- 数据结构 ↔ ORM/模型 ↔ Schema/迁移 ↔ API DTO ↔ 前端类型与展示。
- 后端响应 ↔ 前端解析 ↔ 路由/网关 ↔ 兼容调用方。
- 配置 ↔ 环境变量 ↔ 构建镜像 ↔ Compose/Nginx ↔ 其他服务。
- 多数据库实例的 host、database、environment 和连接用途，避免 A 库的 API 误连 B 库。
- 删除或重命名后的 import、配置、路由、注释和文档残留。

## 5. 数据库、日志与错误处理

- 数据库迁移、恢复、清理和批量写入前，记录并核验 host、database、environment；先备份，并在隔离可丢弃数据库完成演练。
- 修改数据库结构时同步检查 SQLAlchemy 模型、Alembic 迁移、Pydantic Schema、API、前端类型和测试。
- 关键文件创建、数据写入、API/外部服务调用和服务操作应记录可追踪日志，但不得记录密钥、完整 Prompt、令牌或敏感学生数据。
- 面向用户的错误必须是清晰的纯文本，不得暴露 JSON/对象字符串、异常堆栈、SQL、密钥或内部实现。
- 全局异常处理器遇到 dict/object 错误时优先提取 `message`，不得直接用 `str()` 暴露内部对象。

## 6. 自主验收

修改后必须自行验证；失败时定位原因并迭代修复，最多三轮，仍失败则报告完整上下文和已经尝试的步骤。

| 变更类型 | 最低验收要求 |
| --- | --- |
| 仅文档 | 检查相对链接、残留引用、`git diff --check` 和变更范围；无需启动 Docker 或截图 |
| 后端 | 运行相关 pytest/API 验证，检查日志和关联接口回归 |
| 前端非 UI | 运行相关测试、类型检查或生产构建，验证 API 契约 |
| UI/交互 | 使用 Codex 内部浏览器完成真实操作，检查页面、控制台和响应式状态，并保存截图 |
| Docker/配置 | 按变更类型重建或重新加载，检查 `docker compose ps`、健康状态和相关服务日志 |
| 数据库 | 在隔离目标验证迁移与回滚，核对数据完整性、模型和上下游契约 |

容器化规则：

- 修改应用代码后使用 `docker compose up -d --build`，仅 `restart` 不代表加载了新代码。
- 修改 Compose 或环境变量后使用 `docker compose up -d` 重新创建/加载。
- 容器操作后检查 `docker compose ps` 和 Docker Desktop GUI；涉及 UI 时继续做内部浏览器验收。

涉及 UI 的截图、控制台记录和验收日志应放在任务指定的项目目录；任务未指定时使用 `test/acceptance/<task-id>/`。最终报告必须给出可定位的文件路径。只修改文档时不制造无意义截图。

## 7. Prompts 4 执行协议

当用户要求“按照提示词 4 优化”或执行 prompts_4 时：

1. 先完整读取 `docs/prompts_4/README.md`、`docs/prompts_4/00-global-execution-rules.md` 和 `docs/prompts_4/01-implementation-order.md`。
2. 按依赖驱动顺序识别第一个未完成任务；当前顺序从 P0-08 交付验证开始，而不是机械地从 P0-01 开始。
3. 一次只实施一个任务文件。先调查和计划，再测试先行、最小实施、自主验收；本项通过后才能进入下一项。
4. 用户明确授权执行完整优化包时，可以在每项独立验收后继续下一项，无需阶段性停下；数据库、删除、部署、密钥和其他高风险操作仍须获得精确授权。
5. 不并行编辑共享模型、认证、数据库连接或全局配置。若使用子 Agent，只能分派文件范围互不重叠的独立调查或实现，并由主 Agent 复核。
6. HTML 优化报告中的结论、文件和行号必须在当前仓库重验，禁止照抄旧行号盲改。
7. 每项任务使用独立 Conventional Commit；只有代码、测试、浏览器/日志证据均通过后，才更新 `docs/prompts_4/README.md` 的执行状态。
8. 全程保持“出题 Agent + 审题 Agent”两层约束，不得恢复早期 prompts_2 的复杂多 Agent 出题架构。

## 8. Git 与交付

- 开发分支默认使用 `codex/` 前缀；不要直接在受保护的 `main` 上实施变更。
- Commit 使用 `<type>[scope]: <description>`，例如 `docs: refresh agent instructions`。
- 一个逻辑变更对应一个 commit，不混合无关修改。
- 不强制推送，不绕过分支保护；通过 PR 合并时优先使用 Squash，状态检查按仓库当前规则执行。
- 提交或推送前再次检查 `git diff`、`git status`、密钥和生成文件。

最终报告先给结论，再列出：修改文件、验证命令与结果、截图/日志路径、未完成事项，以及必须由用户手动完成的步骤。
