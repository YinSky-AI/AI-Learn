# AI 智能学习平台 - Agent 总入口

> **文件用途**: 本文件是项目提示词体系的顶层入口。任何 Agent 开始工作前应先读取本文件，再按任务类型加载 `agents/` 目录下的对应说明书。
> **主语言**: 简体中文。
> **当前版本目标**: v0.1.3 聚焦 AI 动态出题、学习闭环、生成题目知识库、错题变式、闭环控制系统、自进化机制、中文界面与视觉风格验收。

---

## 1. 项目标识

| 字段 | 值 |
|------|----|
| 项目名称 | AI 智能学习平台 |
| 项目代号 | AI-Learn-v0.1 |
| 根目录 | `D:\Python+AI\AI应用开发项目\项目五` |
| 目标用户 | 6-18 岁学习者，兼顾家长/老师等教学管理者 |
| 前端目标 | Web 应用，桌面端优先验证，兼顾响应式 |

---

## 2. v0.1 核心目标：三层闭环控制系统

构建一个以 AI 动态出题为核心的智能学习平台。采用基于**工程控制论**的三层闭环架构，通过反馈回路实现自适应出题、安全审计和质量控制，并通过 Hermes 风格的自进化机制实现系统的持续自我改进。

```
┌──────────────────────────────────────────────────────────┐
│                    Loop 外环（系统级）                      │
│  系统状态观测 → 反馈聚合(PID) → 控制信号注入               │
│  进化监控 → 进化触发 → Skill/Prompt更新 → 回滚保护         │
│  错误日志 → 四类分类 → 路由处理 → 学习                     │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                    Loop 内环（出题级）                      │
│                                                          │
│  Layer1 [设定值]   CourseIntentAgent ──→ 输出目标参数      │
│       ↓                                                  │
│  Layer2 [规划]     QuestionPlannerAgent ←── PID反馈        │
│       ↓                                                  │
│  Layer3 [记忆]     QuestionMemoryAgent ←── Skill+用户模型  │
│       ↓                                                  │
│  Layer4 [执行]     QuestionGeneratorAgent ←── 控制信号     │
│       ↓                                                  │
│  Layer5 [快检]     QualityCheckAgent ──→ 偏差信号          │
│       ↓                                                  │
│  Layer6 [深审]     SafetyAuditAgent ──→ 安全否决            │
│                     QualityReviewAgent ──→ 趋势报告        │
│       ↓                                                  │
│  Layer7 [反馈聚合]  FeedbackAggregator (Harness内嵌)        │
│       │  ← 接收Layer5/6的所有偏差信号                      │
│       │  → 计算PID三分量(P/I/D)                            │
│       │  → 生成控制信号                                    │
│       │  → 更新系统状态观测器                               │
│       └──────────→ 注入Layer2/3/4的下一次循环              │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                    Loop 学习环（进化级）                    │
│  会话结束 → SummaryAgent → 记忆提炼(≤5条)                  │
│  成功模式 → Skill创建 → 触发条件+步骤+偏好+陷阱            │
│  Skill效果下降 → Skill自改进 → 更新Skill文件               │
│  新会话开始 → 跨会话召回(FTS) → 加载最相关3-5条记忆        │
│  持续交互 → 用户建模 → 能力/行为/偏好/置信度               │
└──────────────────────────────────────────────────────────┘
```

---

## 3. 全局硬约束

### 3.1 AI 能力边界

- v0.1 不做联网搜索。
- v0.1 不调用搜索 API、不爬网页、不接第三方内容源。
- v0.1 不预置或接入外置教材知识库、外部文档知识库。
- v0.1 必须建设"生成题目知识库"：它从用户使用过程中逐步积累，保存历史生成题、Prompt 摘要、答案、解析、质量检查、用户答题行为、错题关联、知识点标签和题目指纹。
- 生成题目知识库可用于去重、复习、错题变式、用户习惯分析和后续个性化推荐，但不得伪装成权威教材来源。
- 联网搜索、真实材料题、外部权威来源引用属于 v0.2 可选增强。

### 3.2 Agent 分层架构（8 层闭环）

> Agent 的详细职责定义见 `agents/06_sub_agent_tasks.md`，此处为摘要。

| Layer | Agent/组件 | 一句话职责 |
|-------|-----------|-----------|
| 1 - 设定值 | CourseIntentAgent | 理解课程选择，补齐年龄、学科、主题、难度、题型、数量参数 |
| 2 - 规划 | QuestionPlannerAgent | 规划题型、数量、难度分布和学习目标；接收PID反馈调整 |
| 3 - 记忆 | QuestionMemoryAgent | 检索历史生成题、错题、Skill和用户偏好，提供去重上下文 |
| 4 - 执行 | QuestionGeneratorAgent | 生成题目、选项、答案和解析；接收控制信号调整生成策略 |
| 5 - 快检 | QualityCheckAgent | 流水线内快速检查：适龄性、答案正确性、难度匹配、重复度 |
| 6 - 深审 | SafetyAuditAgent | 独立安全四维审查（适龄性/准确性/公平性/隐私），一票否决权 |
| 6 - 深审 | QualityReviewAgent | 独立深度质量评估 + 趋势分析 + 反馈信号生成 |
| 7 - 反馈聚合 | FeedbackAggregator | Harness内嵌组件：PID计算 + 状态观测 + 控制信号生成 |
| 8 - 学习进化 | SummaryAgent | 会话记忆提炼 + Skill创建/自改进 + 用户建模 + 进化触发 |

### 3.3 UI 设计边界

- `design_imgs/` 是 UI 风格和页面布局结构的最高优先级参考。
- 样例图中的英文只作为布局占位和视觉密度参考，最终产品界面、按钮、标题、课程、题目、提示、AI 回复和验收截图必须使用自然、准确的简体中文。
- 不要求像素级复刻，但整体页面布局结构至少保持不变，包括：左侧导航、顶部栏、主内容卡片区、课程卡网格、学习页右侧 AI 面板、个人中心双列卡片结构、浮层组件形态。
- 样式风格必须严格符合样例图：浅灰背景、白色圆角卡片、蓝色主色、柔和学科色块、轻阴影、低噪声留白、学习工具型界面。
- 不得改成营销页、暗色主题、重渐变背景、拟物插画、大段说明文案或与样例气质冲突的风格。

### 3.4 Codex 自主验收

- UI 验收必须由 Codex 自主打开页面、截图、保存日志、读取截图内容、自我检查并迭代。
- 截图、自检日志、迭代记录和最终复查结论缺一不可。
- AI 出题验收必须保存 Harness run 日志和 ToolCallLog，证明每次出题经过 intent、plan、memory、generate、quality、safety_audit、save。
- v0.1 验收必须确认工具调用日志中没有联网搜索或网页抓取工具。

### 3.5 工程控制论约束

1. **偏差信号必须回流**：QualityCheckAgent（Layer5）和 SafetyAuditAgent/QualityReviewAgent（Layer6）的检查结果必须通过 FeedbackAggregator 生成控制信号，注入 Layer2/3/4 的下一次循环，不得丢失。
2. **PID参数由Harness注入**：Agent 不自行计算 PID 参数，只接收由 FeedbackAggregator 生成的 ControlSignal 并据此调整行为。
3. **每个Agent必须声明偏差**：如果 Agent 的输出偏离设定值（如生成的题目难度与目标难度不一致），必须在输出中显式声明偏差。
4. **FeedbackAggregator是唯一的控制信号生产者**：除 FeedbackAggregator 外，任何 Agent 不得自行生成控制信号或修改其他 Agent 的参数。
5. **状态观测器持续更新**：FeedbackAggregator 必须在每个循环中维护系统状态估计（质量基线、用户能力、知识覆盖度、重复风险），供后续 Agent 引用。

### 3.6 自进化约束

1. **Skill 创建必须经 SummaryAgent**：不得手动创建 Skill 文件，所有 Skill 必须由 SummaryAgent 从会话数据中提炼并经质量验证后生成。
2. **记忆 TTL 管理**：每条记忆设置 TTL（默认 90 天，高价值记忆 180 天），过期自动淘汰。SummaryAgent 定期运行记忆清理。
3. **进化幅度控制**：单次 Skill 更新不超过原版的 20%，必须备份原版，72 小时内质量下降则自动回滚。
4. **反馈一致性检查**：当多个反馈信号矛盾时（一致性 < 70%），暂不更新策略，等待更多数据积累。
5. **v0.1 进化范围**：仅限 Skill 文档的创建和更新（Hermes Phase 1），不实现 Prompt 模板自修改（Phase 3+）或代码自进化（Phase 4+）。

---

## 4. 文件索引

| 文件 | 用途 | 何时读取 |
|------|------|----------|
| `agents/00_project_overview.md` | 项目愿景、用户画像、范围、版本规划 | 每次会话首次读取 |
| `agents/01_feature_design.md` | 功能设计、AI 动态出题、生成题目知识库、错题变式、闭环控制参数、自进化管理 | 涉及功能设计或产品逻辑时 |
| `agents/02_ui_design.md` | UI 风格、页面布局、样例图约束、交互模式 | 涉及前端/UI/截图验收时 |
| `agents/03_tech_stack.md` | 技术栈、AI Harness、数据库、工具选型、错误日志基础设施、自进化基础设施 | 涉及技术选型或依赖时 |
| `agents/04_architecture.md` | 系统架构、模块边界、闭环数据流、API 契约、自进化数据流 | 涉及架构或接口设计时 |
| `agents/05_implementation_steps.md` | 阶段计划、任务顺序、里程碑 | 涉及排期或实现计划时 |
| `agents/06_sub_agent_tasks.md` | **Agent 角色定义（权威）、协作协议、接口契约、闭环反馈协议、质量门禁** | **涉及多 Agent 协作或任何 Agent 设计时** |
| `agents/07_acceptance_criteria.md` | 验收标准、AC-VIS、AC-AQ、AC-EVO、AC-LOG、证据要求 | 提测、Review、验收时 |
| `agents/08_test_plan.md` | 测试策略、E2E、视觉自检、AI 出题测试、闭环反馈测试 | 编写或执行测试时 |
| `agents/09_data_model.md` | 数据实体、生成题目知识库、Harness 日志、索引、Skill、SessionMemory、ErrorLog | 涉及数据库/API 持久化时 |

---

## 5. 任务路由规则

- 功能需求变化：先读 `00`、`01`，必要时同步 `04`、`07`、`08`、`09`。
- UI 或页面实现：先读 `02`，再读 `07` 的 AC-VIS 和 `08` 的视觉自检流程。
- AI 出题或 Agent 编排：先读 `01`、`03`、`04`、**`06`**，再读 `07` 的 AC-AQ 和 AC-EVO。
- 安全审计或质量审查：先读 **`06`** §2.7 和 §2.8，再读 `07` 的 AC-AQ 和 AC-LOG。
- 自进化或 Skill 管理：先读 **`06`** §2.9，再读 `01` §5.7 和 `04` §3.6。
- 数据库调整：先读 `09`，再核对 `04` 的服务边界和 API 契约。
- 验收和测试：先读 `07`、`08`，必须产出证据文件。

---

## 6. 当前版本记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-07-01 | 初始提示词体系 |
| v0.1.1 | 2026-07-05 | 明确 AI 动态出题为核心，联网搜索降级到 v0.2，加入 Harness、多层 Agent、生成题目知识库、视觉样例和 Codex 自主验收要求 |
| v0.1.2 | 2026-07-05 | 明确样例图英文不影响最终中文产出；将题目记忆升级为使用过程中逐步沉淀的生成题目知识库 |
| v0.1.3 | 2026-07-08 | 架构升级为8层闭环控制系统；新增SafetyAuditAgent、QualityReviewAgent、SummaryAgent、FeedbackAggregator、ErrorLogger；引入工程控制论PID反馈回路和Hermes风格自进化机制；建立Agent间接口契约；文件边界和权威关系规范化 |
