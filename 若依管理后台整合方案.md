# 若依（RuoYi）管理后台整合方案

> 项目：AI-Learn 智能学习平台  
> 版本：v0.2 规划  
> 日期：2026-07-14  
> 状态：待实施

---

## 一、背景与目标

### 1.1 现状

当前 AI-Learn 智能学习平台已完成 v0.1 版本开发，包含以下核心功能：

| 模块 | 说明 |
|------|------|
| 用户端前端 | Next.js 14 + TypeScript + Tailwind CSS + Zustand |
| 业务后端 | FastAPI + SQLAlchemy 2.0 + PostgreSQL + Redis |
| 用户认证 | JWT（HS256），支持 access/refresh token |
| 学习功能 | 课程浏览、学习进度追踪、AI 辅导、成就系统 |
| 部署方式 | Docker Compose（前端 + 后端 + PostgreSQL + Redis） |

### 1.2 痛点

- **无后台管理界面**：课程管理依赖 `seed_data.py` 脚本，用户管理需直接操作数据库
- **无角色权限体系**：User 模型无 role 字段，无法区分管理员与普通用户
- **无操作日志**：无法追踪系统操作记录和异常信息
- **无数据监控**：无法查看系统运行状态（CPU、内存、缓存等）

### 1.3 目标

引入若依（RuoYi）管理后台框架，为平台提供完整的管理能力，同时保持现有用户端系统不变。

---

## 二、技术选型

### 2.1 为什么选择 RuoYi-Vue3-FastAPI

| 对比维度 | 若依原版（Java） | RuoYi-Vue3-FastAPI | 自研管理后台 |
|---------|-----------------|---------------------|-------------|
| 后端语言 | Java (Spring Boot) | **Python (FastAPI)** | Python (FastAPI) |
| 与现有后端兼容性 | ❌ 需维护两套语言 | ✅ 同技术栈 | ✅ 同技术栈 |
| 前端框架 | Vue3 + Element Plus | Vue3 + Element Plus | React (Next.js) |
| 功能完整度 | ★★★★★ | ★★★★☆ | ★★☆☆☆（需从零开发） |
| 开发周期 | 长（需学习 Java 生态） | **短（直接部署+适配）** | 长（需全部自研） |
| 社区支持 | ★★★★★ | ★★★☆☆ | 无 |
| 维护成本 | 高 | **中** | 高 |

**结论**：选择 RuoYi-Vue3-FastAPI，与现有后端技术栈一致，功能开箱即用。

### 2.2 若依版本信息

| 项目 | 内容 |
|------|------|
| 框架名称 | RuoYi-Vue3-FastAPI |
| 当前版本 | v1.6.0 |
| 前端技术 | Vue3 + Element Plus + Vite |
| 后端技术 | FastAPI + SQLAlchemy + MySQL/PostgreSQL + Redis |
| 认证方式 | OAuth2 & JWT |
| 开源地址 | https://gitee.com/yao-zhen/RuoYi-Vue3-FastAPI |
| 默认账号 | admin / admin123 |

### 2.3 整合方式：独立部署

由于用户端前端是 Next.js（React），管理后台前端是 Vue3，两者技术栈不同，采用**独立部署**策略：

- 管理后台作为独立项目，独立前端 + 独立后端
- 两个前端独立部署，共用同一个 PostgreSQL 和 Redis
- 通过 Nginx 统一入口路由分发

---

## 三、架构设计

### 3.1 整体架构

```
                              ┌──────────────────────────────────────────────────┐
                              │             Docker Network: ai-learn-net         │
                              │                                                  │
       Port 80 (Nginx) ────── │──── nginx-gateway ──┬──> frontend:3000          │
                              │                      ├──> backend:8000           │
                              │                      ├──> ruoyi-admin-fe:80       │
                              │                      └──> ruoyi-admin-api:8088    │
                              │                                                  │
       Port 5432 ───────────── │──── postgres:5432                                   │
                              │     ├── schema: public  (现有业务表)                │
                              │     └── schema: ruoyi    (若依系统表)                │
                              │                                                  │
       Port 6379 ───────────── │──── redis:6379                                      │
                              │     ├── db 0: 业务缓存                              │
                              │     └── db 1: 若依缓存/Token                        │
                              └──────────────────────────────────────────────────┘
```

### 3.2 路由设计

| 路由路径 | 目标服务 | 说明 |
|---------|---------|------|
| `/` | Next.js 前端 | 用户端首页 |
| `/_next/*` | Next.js 前端 | 前端静态资源 |
| `/api/v1/*` | FastAPI 后端 | 业务 API |
| `/api/v1/ai/stream` | FastAPI 后端 | SSE 流式输出 |
| `/admin` | 若依 Vue3 前端 | **管理后台入口** |
| `/admin/*` | 若依 Vue3 前端 | 管理后台页面 |
| `/api/admin/*` | 若依 FastAPI 后端 | **管理 API** |

### 3.3 端口分配

| 服务 | 容器内部端口 | 宿主机映射端口 | 说明 |
|------|-------------|---------------|------|
| Nginx Gateway | 80 | **80** | 统一入口，新增服务 |
| Next.js 前端（用户端） | 3000 | 3000 | 保持不变 |
| FastAPI 后端（业务） | 8000 | 8000 | 保持不变 |
| 若依 Vue3 前端（管理后台） | 80 | 不映射 | 仅通过 Nginx 访问 |
| 若依 FastAPI 后端 | 8088 | 不映射 | 仅通过 Nginx 访问 |
| PostgreSQL | 5432 | 5432 | 保持不变 |
| Redis | 6379 | 6379 | 保持不变 |

> 管理后台的前后端容器不对外暴露端口，全部通过 Nginx 统一路由。这样避免端口冲突，也更安全。

### 3.4 目录结构

```
项目根目录/
├── docker-compose.yml              # 主编排文件（修改）
├── nginx/
│   └── nginx.conf                  # Nginx 配置（修改）
├── backend/                         # 业务后端（现有，部分修改）
│   ├── app/
│   │   ├── models/user.py          # [修改] 添加 ruoyi_user_id、is_admin 字段
│   │   ├── core/deps.py            # [修改] Token 兼容逻辑
│   │   ├── core/config.py          # [修改] CORS 配置
│   │   └── services/
│   │       └── ruoyi_sync_service.py  # [新建] 用户同步服务
│   ├── alembic/versions/
│   │   └── xxx_add_ruoyi_fields.py # [新建] 迁移脚本
│   ├── init-db.sql                 # [修改] 添加若依 schema
│   └── seed_data.py                # 保持不变
├── frontend/                        # 业务前端（现有，不修改）
├── ruoyi-admin-backend/             # [新增] 若依 FastAPI 后端
│   ├── Dockerfile
│   ├── .env.prod
│   ├── requirements.txt
│   ├── sql/
│   │   └── ruoyi-pg.sql           # PostgreSQL 版若依 SQL
│   └── app/
│       └── ...                     # 若依后端代码
├── ruoyi-admin-frontend/            # [新增] 若依 Vue3 管理前端
│   ├── Dockerfile
│   ├── nginx.conf                   # 若依前端容器内 Nginx 配置
│   └── ...
└── Makefile                        # [修改] 新增管理相关命令
```

---

## 四、核心设计

### 4.1 PostgreSQL 数据库隔离 — 同库分 Schema

在同一个 PostgreSQL 实例 `learning_platform` 中创建两个 schema，实现数据隔离。

**schema public（现有）**：存放业务表

| 表名 | 说明 |
|------|------|
| users | 用户表 |
| courses | 课程表 |
| lessons | 课时表 |
| user_courses | 用户课程关联 |
| user_lessons | 用户课时完成 |
| chat_messages | AI 对话消息 |
| achievements | 成就表 |
| ... | 其他业务表 |

**schema ruoyi（新增）**：存放若依系统表

| 表名 | 说明 |
|------|------|
| sys_user | 系统用户 |
| sys_role | 角色 |
| sys_menu | 菜单 |
| sys_dept | 部门 |
| sys_user_role | 用户角色关联 |
| sys_role_menu | 角色菜单关联 |
| sys_dict_type | 字典类型 |
| sys_dict_data | 字典数据 |
| sys_config | 系统参数 |
| sys_notice | 通知公告 |
| sys_oper_log | 操作日志 |
| sys_logininfor | 登录日志 |
| sys_job | 定时任务 |
| ... | 其他若依表 |

**初始化 SQL**：

```sql
-- backend/init-db.sql 中添加

-- 现有内容
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pg_trgm";

-- 新增：创建若依专用 schema
CREATE SCHEMA IF NOT EXISTS ruoyi;
GRANT ALL ON SCHEMA ruoyi TO postgres;
```

**若依后端连接配置**：

```env
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/learning_platform
DATABASE_SCHEMA=ruoyi
```

**优势**：完全物理隔离，两套系统的表互不干扰，备份/恢复可按 schema 粒度操作。

### 4.2 Redis 隔离 — db 编号隔离

| Redis db | 使用方 | 说明 |
|----------|--------|------|
| db 0 | 业务系统 | 现有缓存、限流计数器等 |
| db 1 | 若依系统 | 登录 Token、系统配置缓存等 |

**配置**：

```env
# 业务后端（现有，不变）
REDIS_URL=redis://redis:6379/0

# 若依后端（新增）
REDIS_URL=redis://redis:6379/1
```

Redis 默认支持 16 个 db（0-15），足够使用。若依的 Token 存储在 Redis 中，使用 db 1 后与业务缓存完全隔离。

### 4.3 用户表打通方案

两套系统有不同的用户表设计，采用**双向映射 + 事件同步**策略，不合并为一张表。

**两张表的差异**：

| 维度 | 业务 users 表 | 若依 sys_user 表 |
|------|-------------|-----------------|
| 主键 | UUID | bigint |
| 登录名 | email | username |
| 密码算法 | bcrypt | bcrypt（一致） |
| 业务字段 | birth_date, age_group, total_score 等 | 无 |
| 管理字段 | 无 | dept_id, role_ids, status, post_ids 等 |

**双向关联设计**：

```
users (public schema)                 sys_user (ruoyi schema)
┌────────────────────┐               ┌────────────────────┐
│ id: UUID           │◄─────────────│ biz_user_id: UUID   │
│ ruoyi_user_id: INT │─────────────►│ user_id: BIGINT     │
│ is_admin: BOOLEAN  │               │ username: VARCHAR   │
│ email: VARCHAR      │               │ password: VARCHAR   │
│ ...                │               │ ...                 │
└────────────────────┘               └────────────────────┘
```

**业务 users 表新增字段**：

```python
# backend/app/models/user.py

class User(SoftDeleteModel, Base):
    __tablename__ = "users"

    # ... 现有字段保持不变 ...

    # 新增：若依管理后台关联字段
    ruoyi_user_id = Column(Integer, nullable=True, unique=True,
                           comment="若依 sys_user 表的 user_id，用于关联管理后台")
    is_admin = Column(Boolean, default=False, server_default="false",
                     comment="是否为管理员（同步自若依角色）")
```

**若依 sys_user 表新增字段**：

```sql
ALTER TABLE ruoyi.sys_user ADD COLUMN biz_user_id UUID REFERENCES public.users(id);
```

**用户同步服务**（`backend/app/services/ruoyi_sync_service.py`）核心逻辑：

```python
async def sync_user_to_ruoyi(db: AsyncSession, user: User):
    """将业务用户同步到若依 sys_user 表"""
    # 1. 检查是否已同步（通过 ruoyi_user_id 判断）
    # 2. 若未同步，在 ruoyi.sys_user 创建记录，并回填 users.ruoyi_user_id
    # 3. 若已同步，更新 username/nickname 等变更字段

async def sync_admin_status(db: AsyncSession, user: User):
    """同步管理员状态（检查若依角色是否包含 admin）"""
    # 查询 ruoyi.sys_user_role 中是否有 admin 角色
```

**初始管理员账号**：若依系统初始化后自动创建 admin 账号（user_id=1），需在业务 users 表中创建对应记录。

### 4.4 JWT Token 互通方案

两个后端使用**完全相同的 JWT_SECRET_KEY** 签发和验证 Token。

**Token Payload 设计**：

```json
{
    "sub": "uuid-string",          // 统一使用 UUID
    "type": "access",              // access 或 refresh
    "source": "ai-learn",          // 或 "ruoyi"，标识签发方
    "user_name": "nickname",
    "iat": 1234567890,
    "exp": 1234569690
}
```

**密钥统一**：

```env
# 两套后端必须使用完全相同的配置
JWT_SECRET_KEY=your-super-secret-key-change-in-production
JWT_ALGORITHM=HS256
```

**业务后端兼容逻辑**（`backend/app/core/deps.py`）：

```python
async def get_current_user_id(
    credentials: Optional[HTTPAuthorizationCredentials] = Depends(security),
) -> uuid.UUID:
    """统一认证：兼容业务 Token 和若依管理 Token"""
    token = credentials.credentials
    payload = decode_token(token)

    source = payload.get("source", "ai-learn")
    if source == "ruoyi":
        # 通过 ruoyi_user_id 映射找到业务 user_id
        user_id = await resolve_ruoyi_user_to_biz_user(...)
    else:
        user_id = uuid.UUID(payload["sub"])

    return user_id
```

**若依后端兼容逻辑**：Token 验证中间件也需要做相应修改，支持解析 `source == "ai-learn"` 的 Token。

---

## 五、Docker Compose 编排

### 5.1 完整 docker-compose.yml

```yaml
name: ai-learn

services:
  # ========== 现有服务：保持不变 ==========
  postgres:
    image: postgres:16-alpine
    container_name: ai-learn-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: learning_platform
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ai-learn-net

  redis:
    image: redis:7-alpine
    container_name: ai-learn-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ai-learn-net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ai-learn-backend
    env_file:
      - ./backend/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/learning_platform
      REDIS_URL: redis://redis:6379/0
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - ai-learn-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ai-learn-frontend
    ports:
      - "3000:3000"
    networks:
      - ai-learn-net

  # ========== 新增：Nginx 统一网关 ==========
  nginx-gateway:
    image: nginx:alpine
    container_name: ai-learn-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
    depends_on:
      - frontend
      - backend
      - ruoyi-admin-frontend
      - ruoyi-admin-backend
    networks:
      - ai-learn-net

  # ========== 新增：若依 FastAPI 后端 ==========
  ruoyi-admin-backend:
    build:
      context: ./ruoyi-admin-backend
      dockerfile: Dockerfile
    container_name: ai-learn-ruoyi-api
    restart: unless-stopped
    env_file:
      - ./ruoyi-admin-backend/.env.prod
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/learning_platform
      DATABASE_SCHEMA: ruoyi
      REDIS_URL: redis://redis:6379/1
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      JWT_ALGORITHM: HS256
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8088/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - ai-learn-net

  # ========== 新增：若依 Vue3 前端 ==========
  ruoyi-admin-frontend:
    build:
      context: ./ruoyi-admin-frontend
      dockerfile: Dockerfile
    container_name: ai-learn-ruoyi-fe
    restart: unless-stopped
    networks:
      - ai-learn-net

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  ai-learn-net:
    driver: bridge
```

### 5.2 若依后端 Dockerfile

```dockerfile
# ruoyi-admin-backend/Dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8088

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8088"]
```

### 5.3 若依前端 Dockerfile

```dockerfile
# ruoyi-admin-frontend/Dockerfile
# 多阶段构建
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build:prod

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```

---

## 六、Nginx 统一网关配置

```nginx
# nginx/nginx.conf
http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    keepalive_timeout  65;

    # ---- Upstream 定义 ----
    upstream nextjs_frontend {
        server frontend:3000;
        keepalive 32;
    }

    upstream fastapi_backend {
        server backend:8000;
        keepalive 32;
    }

    upstream ruoyi_admin_fe {
        server ruoyi-admin-frontend:80;
        keepalive 32;
    }

    upstream ruoyi_admin_api {
        server ruoyi-admin-backend:8088;
        keepalive 32;
    }

    server {
        listen 80;
        server_name _;

        # ============ 业务前端（Next.js） ============
        location / {
            proxy_pass http://nextjs_frontend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /_next/ {
            proxy_pass http://nextjs_frontend;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
        }

        # ============ 业务 API（FastAPI） ============
        location /api/v1/ {
            proxy_pass http://fastapi_backend;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Connection "";
        }

        # SSE 流式输出（AI 对话）
        location /api/v1/ai/stream {
            proxy_pass http://fastapi_backend;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Connection "";
            proxy_buffering off;
            proxy_cache off;
            chunked_transfer_encoding off;
        }

        # ============ 若依管理后台前端 ============
        location /admin {
            proxy_pass http://ruoyi_admin_fe;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /admin/ {
            proxy_pass http://ruoyi_admin_fe;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # ============ 若依管理 API ============
        location /api/admin/ {
            rewrite ^/api/admin/(.*) /$1 break;
            proxy_pass http://ruoyi_admin_api;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Connection "";
            proxy_connect_timeout 10s;
            proxy_send_timeout 300s;
            proxy_read_timeout 300s;
        }
    }
}
```

**若依前端环境变量**：

```env
# ruoyi-admin-frontend/.env.production
VITE_APP_BASE_API = '/api/admin'
VITE_APP_ROUTER_BASE = '/admin'
```

---

## 七、若依管理后台功能清单

整合完成后，管理后台将提供以下功能：

### 7.1 系统管理（开箱即用）

| 模块 | 功能说明 |
|------|---------|
| 用户管理 | 系统用户 CRUD、状态管理、密码重置 |
| 角色管理 | 角色 CRUD、菜单权限分配、数据权限设置 |
| 菜单管理 | 动态菜单配置、操作权限、按钮权限标识 |
| 部门管理 | 组织架构（公司、部门、小组）配置 |
| 岗位管理 | 用户职务配置 |
| 字典管理 | 系统常用固定数据维护 |
| 参数管理 | 系统动态配置参数 |
| 通知公告 | 系统通知信息发布维护 |

### 7.2 系统监控（开箱即用）

| 模块 | 功能说明 |
|------|---------|
| 操作日志 | 正常操作日志记录和查询 |
| 登录日志 | 登录记录查询，包含异常登录 |
| 在线用户 | 活跃用户状态监控、强制下线 |
| 定时任务 | 在线添加/修改/删除任务调度 |
| 服务监控 | CPU、内存、磁盘、JVM/Python 堆栈监控 |
| 缓存监控 | Redis 缓存信息查询和命令统计 |

### 7.3 系统工具（开箱即用）

| 模块 | 功能说明 |
|------|---------|
| 系统接口 | 根据业务代码自动生成 API 文档 |
| 代码生成 | 配置数据库表一键生成前后端代码 |
| 表单构建 | 拖动表单元素生成 HTML 代码 |

### 7.4 业务扩展（需二次开发）

| 模块 | 功能说明 | 开发方式 |
|------|---------|---------|
| 课程管理 | 课程 CRUD、课时管理、内容编辑 | 若依代码生成 + 二次开发 |
| 学习数据 | 用户学习统计、进度查看 | 自定义页面，查询 public schema |
| AI 提示词管理 | Prompt 模板的在线编辑和管理 | 自定义页面，修改 prompts 目录 |
| 用户画像 | 查看用户行为模型和学习画像 | 自定义页面，查询 behavior_profile |

---

## 八、实施步骤

### 阶段一：环境准备与数据库初始化

**步骤 1**：克隆 RuoYi-Vue3-FastAPI 源码

```bash
git clone https://gitee.com/yao-zhen/RuoYi-Vue3-FastAPI.git
```

**步骤 2**：提取若依代码到项目目录

- 从克隆项目中提取后端代码 → `ruoyi-admin-backend/`
- 从克隆项目中提取前端代码 → `ruoyi-admin-frontend/`

**步骤 3**：适配 PostgreSQL

- 找到若依项目中的 SQL 文件（MySQL 版需转换为 PostgreSQL 语法）
- 给所有 `CREATE TABLE` 语句添加 `SCHEMA ruoyi.` 前缀
- 转换后的 SQL 放置到 `ruoyi-admin-backend/sql/ruoyi-pg.sql`

**步骤 4**：修改 `backend/init-db.sql`，添加若依 schema 创建语句

### 阶段二：若依后端适配

**步骤 5**：修改若依后端环境配置

```env
# ruoyi-admin-backend/.env.prod
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/learning_platform
DATABASE_SCHEMA=ruoyi
REDIS_URL=redis://redis:6379/1
JWT_SECRET_KEY=<与业务后端一致>
JWT_ALGORITHM=HS256
```

**步骤 6**：修改若依 SQLAlchemy 模型，设置默认 schema

```python
class Base(DeclarativeBase):
    metadata = MetaData(schema="ruoyi")
```

或在启动时执行：

```python
async with engine.begin() as conn:
    await conn.execute(text("SET search_path TO ruoyi, public"))
```

### 阶段三：前端适配

**步骤 7**：修改若依前端环境变量和路由配置

```env
# ruoyi-admin-frontend/.env.production
VITE_APP_BASE_API = '/api/admin'
VITE_APP_ROUTER_BASE = '/admin'
```

### 阶段四：Nginx 网关配置

**步骤 8**：按照第六章编写 Nginx 配置文件

### 阶段五：用户打通与认证互通

**步骤 9**：创建 Alembic 迁移脚本，为 users 表添加 `ruoyi_user_id` 和 `is_admin` 字段

**步骤 10**：创建用户同步服务 `backend/app/services/ruoyi_sync_service.py`

**步骤 11**：修改 `backend/app/core/deps.py`，增加若依 Token 兼容逻辑

### 阶段六：Docker Compose 整合

**步骤 12**：编写若依后端和前端 Dockerfile

**步骤 13**：修改 `docker-compose.yml`，新增三个服务

### 阶段七：测试与验证

**步骤 14**：构建并启动所有服务

```bash
docker-compose build
docker-compose up -d
```

**步骤 15**：按验证清单逐项检查

| 验证项 | 预期结果 | 验证方式 |
|--------|---------|---------|
| 业务前端可访问 | `http://localhost:3000` 或 `http://localhost/` 正常显示 | 浏览器 |
| 业务 API 正常 | `http://localhost:8000/health` 返回 healthy | curl |
| 管理后台可访问 | `http://localhost/admin/` 显示若依登录页 | 浏览器 |
| 管理 API 正常 | 若依后端接口正常响应 | curl |
| 数据库隔离 | public schema 有业务表，ruoyi schema 有若依表 | psql |
| Redis 隔离 | db 0 有业务缓存，db 1 有若依 Token | redis-cli |
| 管理员登录 | admin/admin123 可登录管理后台 | 手动测试 |
| Token 互通 | 管理后台 Token 可访问业务 API | curl 带 Token |

---

## 九、文件修改清单

| 文件 | 操作 | 修改内容 |
|------|------|---------|
| `docker-compose.yml` | 修改 | 新增 nginx-gateway、ruoyi-admin-backend、ruoyi-admin-frontend 服务 |
| `nginx/nginx.conf` | 修改 | 新增若依相关 upstream 和 location |
| `backend/init-db.sql` | 修改 | 添加 CREATE SCHEMA ruoyi、初始化管理员账号 |
| `backend/app/models/user.py` | 修改 | 添加 ruoyi_user_id、is_admin 字段 |
| `backend/app/core/deps.py` | 修改 | get_current_user_id 增加若依 Token 兼容 |
| `backend/app/core/config.py` | 修改 | CORS_ORIGINS 添加管理后台地址 |
| `backend/alembic/versions/xxx.py` | 新建 | users 表新增字段的迁移脚本 |
| `backend/app/services/ruoyi_sync_service.py` | 新建 | 用户同步服务 |
| `ruoyi-admin-backend/Dockerfile` | 新建 | 若依后端容器构建 |
| `ruoyi-admin-backend/.env.prod` | 新建 | 若依后端环境配置 |
| `ruoyi-admin-frontend/Dockerfile` | 新建 | 若依前端容器构建 |
| `ruoyi-admin-frontend/.env.production` | 修改 | VITE_APP_BASE_API、路由 base |
| `ruoyi-admin-backend/sql/ruoyi-pg.sql` | 新建/适配 | PostgreSQL 版若依初始化 SQL |
| `Makefile` | 修改 | 新增 ruoyi 相关的 make 命令 |

---

## 十、风险与应对策略

| 风险 | 影响 | 应对策略 |
|------|------|---------|
| 若依 SQL 适配不完整 | 数据库初始化失败 | 优先使用官方 PostgreSQL 版 SQL；先用独立 PG 容器跑通再整合 |
| SQLAlchemy schema 配置问题 | 若依后端读写错误 | 优先用 `SET search_path` 方式，改动最小 |
| JWT payload 结构不同 | Token 验证失败 | 确保 SECRET_KEY 一致；解析函数做 `sub` 字段 UUID 兼容 |
| 端口冲突 | 服务无法启动 | 管理后台容器不映射宿主机端口，仅 Nginx 访问 |
| 数据迁移影响现有数据 | 业务系统异常 | 新字段设 nullable=True，不影响现有数据；先测试环境验证 |
| 若依社区版本更新滞后 | 功能缺失或 Bug | 关注官方仓库更新；关键功能可自行 patch |

---

## 十一、可选优化方案

### 11.1 若依前端静态文件由 Nginx Gateway 直接托管

减少一个容器，将若依前端构建产物直接放在宿主机上，由 Nginx Gateway 提供静态文件服务：

```nginx
location /admin {
    alias /data/ruoyi-admin/dist/;
    try_files $uri $uri/ /admin/index.html;
}
```

### 11.2 Redis Key 前缀隔离

不使用 db 编号，改用 key 前缀隔离：

- 业务系统：`biz:rate:xxx`、`biz:cache:xxx`
- 若依系统：`ruoyi:login_tokens`、`ruoyi:sys_config:xxx`

更加灵活，但需修改两边的 Redis 客户端配置。

### 11.3 单 Schema 方案

对隔离性要求不高时，所有表放在 `public` schema 下，通过表名前缀区分（若依表自带 `sys_` 前缀，与业务表天然隔离）。配置更简单，但表多了容易混乱。

---

## 参考资料

- [RuoYi-Vue-FastAPI（Gitee）](https://gitee.com/lanjue/RuoYi-Vue-FastAPI)
- [RuoYi-Vue3-FastAPI（Gitee）](https://gitee.com/if-according-to-the-framework_1/ruo-yi-vue3-fast-api)
- [若依官方文档](http://doc.ruoyi.vip/)
- [若依在线体验](https://vfadmin.insistence.tech/)（账号：admin / admin123）
